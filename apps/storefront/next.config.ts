import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // تصاویر فعلاً SVG محلی‌اند؛ هنگام اتصال بک‌اند واقعی، remotePatterns برای object storage اضافه شود.
};

export default nextConfig;
